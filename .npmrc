# The workspace targets Node 24 (see .nvmrc / package.json engines). Keep the
# engine check advisory so contributors on a slightly older LTS can still
# install and run the toolchain locally; CI always runs the pinned version.
engine-strict=false
auto-install-peers=true
strict-peer-dependencies=false
