$TMP/config.yaml is invalid (5 problems):
  - endpoints[0].listen: must be host:port, e.g. 127.0.0.1:8443, got "not-a-listen-address"
  - endpoints[0].name: must be lower-case letters, digits or dashes starting with a letter or digit (max 63 chars), got "Bad Name"
  - endpoints[0].trustedEvidence[0]: is not an evidenceDigest (expected sha256:<64 hex> — the form the CLI prints — or sha256/<43 canonical base64url chars>, sha256/<64 hex>, bare 64 hex), got "not-a-digest"
  - endpoints[0].upstream: must be an https:// base URL without path, query or fragment, got "http://insecure.example"
  - log.level: must be one of debug, info, warn, error, got "shouty"
