/** Where the platform publishes an endpoint's evidence bundle (ADR-002). */
export const EVIDENCE_PATH_SUFFIX = '/.well-known/swarm-evidence';

/** Header the gatekeeper puts its verdict in on every proxied response. */
export const VERDICT_HEADER = 'x-gatekeeper-verdict';
